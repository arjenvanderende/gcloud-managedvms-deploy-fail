{
  "name": "gcloud-deploy-fail",
  "description": "Reproduction case of deployment failure on Google ManagedVMs due to invalid filename",
  "version": "1.0.0",
  "engines": {
    "node": ">=0.10.x <0.12"
  },
  "scripts": {
    "start": "node src/app.js"
  },
  "dependencies": {
    "appengine": "~0.3.0",
    "express": "~4.13.3",
    "express-domain-middleware": "~0.1.0"
  },
  "devDependencies": {
    "istanbul": "^0.3.0"
  }
}
